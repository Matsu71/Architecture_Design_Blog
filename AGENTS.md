# Architecture Japan Guide — 開発方針

最終目標は、日本全国の注目すべき建築を、正確な情報とともに探せる「世界で一番見やすく、わかりやすい建築まとめサイト」。対象は日本、関東を最優先にする。掲載件数の区切りを最終目標の達成扱いにしない。

- 既存のChatGPT製構成を良いものと仮定しない。維持ありきでなく、建築・関連情報サイトを比較して構成を選ぶ。参考先・採用点・不採用点を `docs/REFERENCE_DESIGN_2026-09-14.md` に記録する。
- トップは検索と地図・一覧を優先。記事の題名は建物名、見出しは短い具体語。本文以外に長い抽象的な宣伝文を置かない。他サイトの文章・図・固有デザインは複製しない。
- 地図は建物ごとの座標を保つ。件数クラスタ、地域中心への仮置き、重複を隠すための座標ずらしは禁止。近接時は候補選択と拡大で解決する。位置不明は一覧に残し地図には出さない。
- 名称、竣工年、設計、住所、構造・素材には出典を持たせる。竣工／開館、設計／内装／構造、創建／増築／改修を混同しない。検証日は実際に資料を確認した日だけ更新する。
- 基本情報、施設座標、棟位置、入口、見学情報の確認状態を分ける。施設座標だけで全情報検証済みと表示しない。原材料の種類・産地・製法を見た目から推測しない。
- 写真は権利と利用条件を確認してから。許諾不明の画像や実物に見える架空画像で埋めない。
- `data/buildings.json` が編集用の正本。変更後は `npm run build:articles` で静的記事、一覧、軽量地図索引を更新し、`npm test` と `npm run validate` を実行する。
- `npm test` は生成物の一致も確認する。DOMテストと実地・本番確認を混同しない。`tests/dom_check.py` は通信・地図ライブラリを代用するテストであり、実際の地図の正確性を証明しない。
- ホスト用の成果物はルート配信を維持する。文書は `docs/`。既存ファイルと同時進行の変更を確認し、関連変更をまとめて反映する。
